# Screenshots

Screenshots of the live dashboard are captured manually by the developer after
running `flutter run -d chrome` per the steps in `doc/RUNBOOK.md`.

Add a PNG here named `dashboard-<date>.png` and reference it from
`README.md` once captured.
